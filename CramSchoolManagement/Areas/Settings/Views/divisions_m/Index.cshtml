@model IEnumerable<CramSchoolManagement.Areas.Settings.Models.divisions_m>

@{
    ViewBag.Title = "学校区分一覧";
    Layout = "~/Areas/Settings/Views/Shared/_Settings.cshtml";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("学校区分新規作成", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("編集", "Edit", new { id=item.division_id }) |
            @Html.ActionLink("削除", "Delete", new { id=item.division_id })
        </td>
    </tr>
}

</table>
