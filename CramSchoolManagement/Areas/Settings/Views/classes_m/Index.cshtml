@model IEnumerable<CramSchoolManagement.Areas.Settings.Models.classes_m>

@{
    ViewBag.Title = "教科一覧";
    Layout = "~/Areas/Settings/Views/Shared/_Settings.cshtml";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("教科新規作成", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.divisions_m.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.eng_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.divisions_m.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.eng_name)
        </td>
        <td>
            @Html.ActionLink("編集", "Edit", new { id=item.class_id }) |
            @Html.ActionLink("削除", "Delete", new { id=item.class_id })
        </td>
    </tr>
}

</table>
