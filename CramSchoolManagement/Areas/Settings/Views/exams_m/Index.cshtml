@model IEnumerable<CramSchoolManagement.Areas.Settings.Models.exams_m>

@{
    ViewBag.Title = "テスト区分一覧";
    Layout = "~/Areas/Settings/Views/Shared/_Settings.cshtml";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("テスト区分新規作成", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("編集", "Edit", new { id=item.exam_id }) |
            @Html.ActionLink("削除", "Delete", new { id=item.exam_id })
        </td>
    </tr>
}

</table>
